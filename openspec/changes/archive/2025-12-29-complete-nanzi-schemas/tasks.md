## 1. Schema 补全
- [ ] 1.1 修改 `app/api/v1/schemas/data.py`，根据 `CLICKHOUSE_TABLES.md` 补全 `YunshuRoomResponse` 的字段。
- [ ] 1.2 补全 `YunshuRackResponse` 的字段。
- [ ] 1.3 补全 `YunshuDevicePointResponse` 的字段。
- [ ] 1.4 为新增字段添加中文 `description` 和（如果可能）`example`。

## 2. 验证
- [ ] 2.1 运行测试，确保字段解析无误。
